git is one of the vcs tool,which is used to track the devolopers code as well as to version the code \
it is a platform indipendent and it is free to use .git is having features like baranching kind of concepts

